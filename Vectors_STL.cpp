#include <bits/stdc++.h>
using namespace std;

void explainVector(){
    // vector <int> v; 
    // v.push_back(1);
    // v.emplace_back(2);
    // vector <pair <int, int>> v;
    // v.push_back({1,2});
    // v.emplace_back(1,2);

    // MAKING 

    vector <int> v (5,100);
    vector <int> vv(5);
    vector <int> v1 (5,20);
    vector <int> v2(v1);  
    
}